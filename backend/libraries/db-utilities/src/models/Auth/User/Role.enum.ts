export enum Role {
  Premium = 'premium',
  Basic = 'basic'
}

export enum Access {
  airline = 'Airline',
  retail = 'Retail',
  resturant = 'Resturant',
  rewardStore = 'RewardStore'
}

export enum RewardPercentage {
  basicReward = 5,
  premiumReward = 10
}

export enum AuthStratagy {
  jwt = 'JWT',
  google = 'googleOAuth',
  azure = 'azureOAUth'
}
